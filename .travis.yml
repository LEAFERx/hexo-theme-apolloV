language: node_js
node_js: stable

install:
  - npm install

before_script:
  - npm install -g gulp

script:
  - npm run build

after_script:
  - cd ./dist
  - git init
  - git config user.name "LEAFERx"
  - git config user.email "leaferx@outlook.com"
  - git add .
  - git commit -m "Update"
  - git push --force "https://${GH_TOKEN}@${GH_REF}" master:master
  # - cd ~
  # - 'sed -i "s/base: ''/''/base: ''/vuetest/''/" src/router/index.js'

branches:
  only:
    - dev

env:
  global:
    - GH_REF: github.com/LEAFERx/hexo-theme-vuetest.git
